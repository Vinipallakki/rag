google-generativeai==0.8.3   # Gemini API client
chromadb==0.5.5              # Vector DB for embeddings
tqdm==4.66.5                 # Progress bars
pdfplumber==0.11.4           # For extracting text from PDFs
python-docx==1.1.2           # For extracting text from Word files
pandas==2.2.3                # Optional, for working with structured data
# LangChain and core utilities
langchain>=0.1.0

# Google Gemini (Generative AI) client
google-generativeai>=0.1.0

# Vector DB
chromadb>=0.3.32

# Optional: For OpenAI LLM testing (if needed)
openai>=0.27.0

# Standard utilities
numpy>=1.24.0
tqdm>=4.65.0
